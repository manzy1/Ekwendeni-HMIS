<!doctype html><html><head><meta charset="utf-8"><title>Family Planning</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"></head><body class="bg-light"><main class="container py-4"><div class="d-flex justify-content-between"><h1 class="h3">Family Planning Register — {{ $year }}</h1><a class="btn btn-success" href="{{ route('family-planning-visits.create') }}">Record visit</a></div>@if(session('success'))<div class="alert alert-success mt-3">{{ session('success') }}</div>@endif<div class="card card-body my-3"><strong>Method mix</strong><div>@foreach($methodMix as $item)<span class="badge text-bg-success me-2">{{ $item->current_method }}: {{ $item->total }}</span>@endforeach</div></div><table class="table bg-white"><thead><tr><th>Date</th><th>Serial</th><th>Patient</th><th>Method</th><th>Visit</th><th>Next appointment</th></tr></thead><tbody>@forelse($visits as $visit)<tr><td>{{ $visit->visit_date->format('d M Y') }}</td><td>{{ $visit->monthly_serial_number }}</td><td>{{ $visit->patient->full_name }}</td><td>{{ $visit->current_method }}</td><td>{{ $visit->visit_type }}</td><td>{{ $visit->next_appointment?->format('d M Y') }}</td></tr>@empty<tr><td colspan="6" class="text-center">No FP visits recorded.</td></tr>@endforelse</tbody></table></main></body></html>
